apiVersion: application.epinio.io/v1
kind: AppChart
metadata:
  name: traefiked-0-2-0
spec:
  shortDescription: Traefik-native ingress
  description: Fork of epinio default AppChart with Traefik-native ingressroute
  helmChart: https://github.com/J4NS-R/epinio-app-charts/releases/download/v0.2.0/epinio-application-traefiked-0.2.0.tgz
  settings:
    # See Chart values.yaml for an example
    traefik:
      type: string
    priorityClassName:
      type: string
